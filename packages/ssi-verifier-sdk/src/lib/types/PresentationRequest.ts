/**
 * Options for creating a presentation request
 */
export interface PresentationRequestOptions {
  baseUrl?: string;
  clientId?: string;
  scope?: string;
  redirectUri?: string;
  responseMode?: string;
}
